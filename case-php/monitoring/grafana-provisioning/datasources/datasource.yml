apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # URL do serviço do Prometheus, como definido no docker-compose.
    url: http://prometheus:9090
    isDefault: true